---
import { pwaInfo } from 'virtual:pwa-info'

export interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0,viewport-fit=cover">
    <link rel="icon" type="image/svg+xml" href="/icon.svg">
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" sizes="180x180">
    <link rel="mask-icon" href="/icon.svg" color="#FFFFFF">
    <meta name="theme-color" content="#212129">
    <meta name="generator" content={Astro.generator}>
    <title>{title}</title>
    <meta name="description" content="A Minimal web UI for GeminiPro Chat.">
    { import.meta.env.HEAD_SCRIPTS && <Fragment set:html={import.meta.env.HEAD_SCRIPTS } /> }
    { pwaInfo && <Fragment set:html={pwaInfo.webManifest.linkTag} /> }
    { import.meta.env.PROD && pwaInfo && <Fragment set:html={pwaInfo.registerSW.scriptTag} /> }
  </head>
  <body>
    <slot />
  </body>
</html>

<style is:global>
  /* Claude for Chrome font set (public/fonts) */
  @font-face {
    font-family: 'Styrene B LC';
    src: url('/fonts/StyreneBLC-Regular-CGpoA792.woff2') format('woff2');
    font-weight: 400;
    font-style: normal;
    font-display: swap;
  }
  @font-face {
    font-family: 'Styrene B LC';
    src: url('/fonts/StyreneBLC-RegularItalic-01Bls9EJ.woff2') format('woff2');
    font-weight: 400;
    font-style: italic;
    font-display: swap;
  }
  @font-face {
    font-family: 'Styrene B LC';
    src: url('/fonts/StyreneBLC-Medium-B_2DtlvT.woff2') format('woff2');
    font-weight: 500;
    font-style: normal;
    font-display: swap;
  }
  @font-face {
    font-family: 'Styrene B LC';
    src: url('/fonts/StyreneBLC-MediumItalic-BBL8uUgC.woff2') format('woff2');
    font-weight: 500;
    font-style: italic;
    font-display: swap;
  }

  @font-face {
    font-family: 'Tiempos Text';
    src: url('/fonts/tiempostext-regular-webfont-fvbKcKMl.woff2') format('woff2');
    font-weight: 400;
    font-style: normal;
    font-display: swap;
  }
  @font-face {
    font-family: 'Tiempos Text';
    src: url('/fonts/tiempostext-regularitalic-webfont-DrlEWHEs.woff2') format('woff2');
    font-weight: 400;
    font-style: italic;
    font-display: swap;
  }
  @font-face {
    font-family: 'Tiempos Text';
    src: url('/fonts/tiempostext-semibold-webfont-CKUQAWup.woff2') format('woff2');
    font-weight: 600;
    font-style: normal;
    font-display: swap;
  }
  @font-face {
    font-family: 'Tiempos Text';
    src: url('/fonts/tiempostext-semibolditalic-webfont-CtU2xEIm.woff2') format('woff2');
    font-weight: 600;
    font-style: italic;
    font-display: swap;
  }

  @font-face {
    font-family: 'Copernicus';
    src: url('/fonts/Copernicus-Book-BOzndwED.woff2') format('woff2');
    font-weight: 400;
    font-style: normal;
    font-display: swap;
  }
  @font-face {
    font-family: 'Copernicus';
    src: url('/fonts/Copernicus-BookItalic-CgcPgk0A.woff2') format('woff2');
    font-weight: 400;
    font-style: italic;
    font-display: swap;
  }
  @font-face {
    font-family: 'Copernicus';
    src: url('/fonts/Copernicus-Medium-CbocBCx3.woff2') format('woff2');
    font-weight: 500;
    font-style: normal;
    font-display: swap;
  }
  @font-face {
    font-family: 'Copernicus';
    src: url('/fonts/Copernicus-MediumItalic-CtolQ55x.woff2') format('woff2');
    font-weight: 500;
    font-style: italic;
    font-display: swap;
  }

  @font-face {
    font-family: 'JetBrains Mono';
    src: url('/fonts/JetBrainsMono-Regular-BQaDgvhP.woff2') format('woff2');
    font-weight: 400;
    font-style: normal;
    font-display: swap;
  }
  @font-face {
    font-family: 'FangZhengLanTingKanSong';
    src: url('/fonts/方正兰亭刊宋_GBK.TTF') format('truetype');
    font-weight: normal;
    font-style: normal;
    font-display: swap;
  }

  /* Back-compat: keep previous TiemposText family name */
  @font-face {
    font-family: 'TiemposText';
    src: url('/fonts/TiemposText-Regular.ttf') format('truetype');
    font-weight: 400;
    font-style: normal;
    font-display: swap;
  }

  :root {
    --c-bg: #fbfbfb;
    --c-fg: #444444;
    --c-scroll: #d9d9d9;
    --c-scroll-hover: #bbbbbb;
    scrollbar-color: var(--c-scrollbar) var(--c-bg);
  }

  html {
    font-family: var(--font-ui, system-ui, sans-serif);
    background-color: var(--c-bg);
    color: var(--c-fg);
  }

  html.dark {
    --c-bg: #212129;
    --c-fg: #ddddf0;
    --c-scroll: #333333;
    --c-scroll-hover: #555555;
  }

  main {
    max-width: 70ch;
    min-height: calc(100vh - 4rem);
    margin: 0 auto;
    padding: 6rem 2rem 4rem;
  }

  ::-webkit-scrollbar {
    width: 6px;
    height: 6px;
  }
  ::-webkit-scrollbar-thumb {
    background-color: var(--c-scroll);
    border-radius: 4px;
  }
  ::-webkit-scrollbar-thumb:hover {
    background-color: var(--c-scroll-hover);
  }
  ::-webkit-scrollbar-track {
    background-color: var(--c-bg);
  }

  @media screen and (max-width: 768px) {
    main {
      min-height: calc(100vh - 4.5rem);
    }
  }
  ::view-transition-old(root),
  ::view-transition-new(root) {
    animation: none;
    mix-blend-mode: normal;
  }

  /* Always place the new root on top so
     element animations (like the theme icon) are visible in both directions */
  ::view-transition-old(root) {
    z-index: 1;
  }

  ::view-transition-new(root) {
    z-index: 999;
  }

  /* Claude-style font system */
  :root {
    /* UI sans (Claude uses Styrene B LC) */
    --font-ui: 'Styrene B LC', system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
    /* Assistant response serif (Claude uses Tiempos Text) */
    --font-response: 'Tiempos Text', Georgia, Cambria, 'Times New Roman', Times, serif;
    /* Optional UI serif (Claude sometimes uses Copernicus) */
    --font-ui-serif: 'Copernicus', Georgia, Cambria, 'Times New Roman', Times, serif;
    /* Monospace (Claude uses JetBrains Mono fallback) */
    --font-mono: 'JetBrains Mono', ui-monospace, 'SF Mono', Monaco, 'Cascadia Code', 'Roboto Mono', Menlo, Consolas, 'Courier New', monospace;
  }

  /* Per-role chat fonts */
  .message.msg-assistant { font-family: var(--font-response); }
  .message.msg-user { font-family: var(--font-ui); }

  /* Chinese glyph override remains serif-friendly */
  .message.msg-assistant :lang(zh),
  .message.msg-assistant :lang(zh-CN),
  .message.msg-assistant :lang(zh-TW) {
    font-family: 'FangZhengLanTingKanSong', var(--font-response);
  }

  /* Code font across roles */
  .message pre,
  .message code,
  .message kbd,
  .message samp { font-family: var(--font-mono); }
</style>

<script>
const initTheme = () => {
  const darkSchema
    = window.matchMedia
    && window.matchMedia('(prefers-color-scheme: dark)').matches
  const storageTheme = localStorage.getItem('theme')
  if (storageTheme) {
    document.documentElement.classList.toggle(
      'dark',
      storageTheme === 'dark',
    )
  } else {
    document.documentElement.classList.toggle('dark', darkSchema)
  }
}

initTheme()
</script>
